#include"thuvien.h"
int main() {
	/*
	Login* headsv = NULL, * headgv = NULL;
	docfilesv(headsv);
	docfilegv(headgv);
	int tmp;
	tmp=newlogin(headsv, headgv);
	system("pause");
	return 0;
	
	year* head = NULL;
	themnamhoc(head);
	dslop* tmp = head->dslop;
	while (tmp != NULL) {
		cout << "danh sach lop ";
		cout << tmp->tenlop << endl;
		thongtinsv* tmp1 = tmp->thongtinsv;
		while (tmp1 != NULL) {
			cout << tmp1->tensv << endl;
			tmp1 = tmp1->next;
		}
		tmp = tmp->next;
	}
	*/
	/*
	hocki* head = NULL;
	them1hocki(head);
	ttsv* tmp = head->monhoc->ttsv;
	Xuat(tmp);*/
}